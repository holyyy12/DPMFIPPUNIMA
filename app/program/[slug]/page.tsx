import Link from 'next/link';
import { ArrowLeft, CheckCircle2, Images, Play } from 'lucide-react';
import { PublicFrame } from '@/components/v4-public';
import { programs } from '@/lib/site-content';
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=programs.find(x=>x.slug===slug)??programs[0];return <PublicFrame><section className="v5-shell v5-program-detail"><Link href="/program"><ArrowLeft/> Semua Program</Link><div className="v5-program-detail-hero" style={{backgroundImage:`linear-gradient(90deg,#043d30dd,#043d3040),url(${p.image})`}}><span>{p.media==='video'?<Play/>:<Images/>}{p.unit}</span><h1>{p.title}</h1><p>{p.copy}</p></div><div className="v5-program-detail-grid"><article><h2>Publikasi Program</h2><p>Dokumentasi program dapat berupa foto, video, gambar, laporan, dan pembaruan berkala.</p><div className="v5-inline-gallery">{programs.map(x=><span key={x.slug} style={{backgroundImage:`url(${x.image})`}}/>)}</div></article><aside><h2>Progress</h2><strong>{p.progress}%</strong><i><em style={{width:`${p.progress}%`}}/></i><p><CheckCircle2/> Indikator keberhasilan: <b>{p.success}%</b></p><p><CheckCircle2/> Pembaruan terakhir: 20 Mei 2026</p></aside></div></section></PublicFrame>}
